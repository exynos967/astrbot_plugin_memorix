{
  "scope": {
    "type": "object",
    "description": "记忆共享范围",
    "hint": "决定机器人的记忆在哪些聊天之间是互通的。",
    "items": {
      "mode": {
        "type": "string",
        "description": "记忆共享模式",
        "hint": "platform_global：同一个平台上所有会话共享一份记忆；user_global：按用户隔离；group_global：按群隔离，私聊为用户隔离；umo：每个聊天窗口独立记忆。",
        "default": "group_global",
        "options": [
          "platform_global",
          "user_global",
          "group_global",
          "umo"
        ]
      }
    }
  },
  "provider": {
    "type": "object",
    "description": "LLM 模型选择",
    "hint": "Memorix 的总结生成、人物画像提取等功能需要调用大语言模型。留空使用当前会话默认的 Provider。",
    "items": {
      "chat_provider_id": {
        "type": "string",
        "description": "聊天模型 Provider ID",
        "hint": "插件的总结、画像生成等 LLM 任务将使用此模型（留空则使用当前会话的默认模型）。",
        "default": "",
        "_special": "select_provider",
        "obvious_hint": true
      }
    }
  },
  "embedding": {
    "type": "object",
    "description": "向量嵌入（Embedding）—— 控制文本向量化方式，直接影响语义检索质量",
    "hint": "Embedding 将文本转换为向量，是语义检索的核心。关闭时使用本地确定性向量回退，开启后使用 OpenAI 兼容服务，检索效果会显著提升。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用 Embedding model",
        "hint": "关闭：使用本地确定性向量，但语义检索精度有限。开启：调用你配置的 OpenAI 兼容 API。开启前请确保下方的 API 地址和密钥已正确填写。",
        "default": false,
        "obvious_hint": true
      },
      "dimension": {
        "type": "int",
        "description": "向量维度",
        "hint": "Embedding向量维度必须和你使用的 Embedding 模型匹配，否则会导致索引错误。若不确定请查阅模型文档。",
        "default": 1024
      },
      "batch_size": {
        "type": "int",
        "description": "每次处理的文本条数",
        "hint": "每次发给 API 处理多少条文本。调大可以加快速度，但单次请求会变慢。一般保持默认，建议范围 8~64。",
        "default": 32
      },
      "max_concurrent": {
        "type": "int",
        "description": "最大并发请求数",
        "hint": "同时向 API 发多少个请求。调大能加快批量处理速度，过大可能被 API 限流。一般保持默认即可。",
        "default": 5
      },
      "openapi": {
        "type": "object",
        "description": "Embedding API 配置",
        "hint": "填写 OpenAI 兼容格式的 Embedding API 信息；启用远程 Embedding 时不会从环境变量兜底读取。",
        "items": {
          "base_url": {
            "type": "string",
            "description": "API 地址",
            "hint": "启用远程 Embedding 时必填。Embedding 服务的 API 地址，末尾不需要加 /v1，插件会自动补上。",
            "default": ""
          },
          "api_key": {
            "type": "string",
            "description": "API 密钥",
            "hint": "你的 API Key，从对应服务商处获取。",
            "default": ""
          },
          "model": {
            "type": "string",
            "description": "模型名称",
            "hint": "启用远程 Embedding 时必填。要使用的 Embedding 模型名，具体名称请查看你的服务商文档。",
            "default": ""
          },
          "timeout_seconds": {
            "type": "float",
            "description": "请求超时时间（秒）",
            "hint": "单次请求最多等待多少秒。",
            "default": 30.0
          },
          "max_retries": {
            "type": "int",
            "description": "请求失败重试次数",
            "hint": "请求失败后最大重试次数。设为 0 表示不重试。建议 2~5 次。",
            "default": 3
          }
        }
      }
    }
  },
  "retrieval": {
    "type": "object",
    "description": "记忆检索",
    "hint": "检索配置影响 LLM 接收到的上下文质量。合理的参数能在召回率和精度之间取得平衡。",
    "items": {
      "top_k_final": {
        "type": "int",
        "description": "最终回忆的记忆条数",
        "hint": "每次 LLM 请求时，最终注入到上下文中的记忆条数上限。数值越大上下文越丰富，但会消耗更多 Token。建议范围 3~20。",
        "default": 10
      },
      "auto_inject": {
        "type": "object",
        "description": "请求前自动注入",
        "hint": "在每次 LLM 请求前自动检索当前聊天相关的长期记忆，并作为内部参考附加到当前用户消息的额外内容中，避免破坏稳定 system prompt 缓存。命中为空时不会注入。",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用自动注入长期记忆",
            "hint": "开启后，机器人在回复前会根据当前消息自动查一次长期记忆；关闭后仍可通过 search_memory 工具按需检索。",
            "default": true
          },
          "top_k": {
            "type": "int",
            "description": "自动注入条数",
            "hint": "每次自动注入最多包含多少条记忆。建议 3~10，过大会增加上下文长度。",
            "default": 5
          },
          "min_query_chars": {
            "type": "int",
            "description": "最短触发字数",
            "hint": "当前消息少于该长度时不自动检索，避免“嗯”“哈哈”等低信息量消息触发无意义召回。",
            "default": 4
          }
        }
      },
      "enable_ppr": {
        "type": "bool",
        "description": "启用 PageRank 图谱重排",
        "hint": "开启后，搜索结果会根据记忆之间的关联程度重新排序，让联系更紧密的记忆排在前面，建议保持开启。",
        "default": true
      },
      "enable_parallel": {
        "type": "bool",
        "description": "启用并行搜索",
        "hint": "开启后，向量检索和稀疏检索（BM25）将并行执行，可降低检索延迟。",
        "default": true
      },
      "vector_pools": {
        "type": "object",
        "description": "双向量池检索",
        "hint": "single 使用单一向量池，保持旧行为；dual 将段落向量和图谱向量分离，启用前必须先运行 scripts/migrate_vectors_to_dual_pools.py 生成 ready manifest。",
        "items": {
          "mode": {
            "type": "string",
            "description": "向量池模式",
            "hint": "默认 dual，与上游 A_memorix 对齐；dual 需要先完成离线迁移，否则启动时会自动降级为 single。",
            "default": "dual",
            "options": [
              "single",
              "dual"
            ]
          },
          "paragraph_top_k": {
            "type": "int",
            "description": "段落池召回上限",
            "hint": "dual 模式下从段落向量池召回的候选数量。",
            "default": 20
          },
          "graph_top_k": {
            "type": "int",
            "description": "图谱池召回上限",
            "hint": "dual 模式下从关系/实体向量池召回的候选数量。",
            "default": 40
          },
          "graph_expand_paragraph_k": {
            "type": "int",
            "description": "图谱扩展段落上限",
            "hint": "通过图谱命中向外扩展关联段落时最多保留多少候选。",
            "default": 80
          },
          "relation_expand_per_hit": {
            "type": "int",
            "description": "每个关系命中扩展数",
            "hint": "每个关系向量命中最多扩展多少条关联段落。",
            "default": 5
          },
          "entity_expand_per_hit": {
            "type": "int",
            "description": "每个实体命中扩展数",
            "hint": "每个实体向量命中最多扩展多少条关联段落。",
            "default": 8
          },
          "relation_evidence_weight": {
            "type": "float",
            "description": "关系证据权重",
            "hint": "关系向量命中映射为段落证据时的基础权重。",
            "default": 1.0
          },
          "entity_evidence_weight": {
            "type": "float",
            "description": "实体证据权重",
            "hint": "实体向量命中映射为段落证据时的基础权重。",
            "default": 0.55
          },
          "semantic_weight": {
            "type": "float",
            "description": "语义权重",
            "hint": "dual 融合时段落语义召回的权重。",
            "default": 0.65
          },
          "sparse_weight": {
            "type": "float",
            "description": "稀疏权重",
            "hint": "dual 融合时 BM25/稀疏召回的权重。",
            "default": 0.2
          },
          "graph_weight": {
            "type": "float",
            "description": "图谱权重",
            "hint": "dual 融合时图谱向量召回的权重。",
            "default": 0.15
          },
          "score_calibration_method": {
            "type": "string",
            "description": "双池分数校准方法",
            "hint": "none 保持原始加权；minmax / rank_percentile / weighted_rrf 等会在查询内校准各通道分数后再融合。",
            "default": "none",
            "options": [
              "none",
              "minmax",
              "rank_percentile",
              "robust_sigmoid",
              "softmax",
              "weighted_rrf",
              "semantic_minmax",
              "semantic_rrf",
              "semantic_sparse_rrf",
              "semantic_graph_rrf"
            ]
          },
          "score_calibration_rrf_k": {
            "type": "int",
            "description": "RRF 校准常数",
            "hint": "仅在 RRF 类校准方法下生效，值越大排名差异越平滑。",
            "default": 60
          },
          "relation_intent": {
            "type": "object",
            "description": "关系意图权重",
            "hint": "问题命中关系意图时，dual 模式下使用的图谱候选数与融合权重。",
            "items": {
              "graph_top_k": {
                "type": "int",
                "description": "关系意图图谱候选数",
                "hint": "关系意图命中时从图谱向量池召回的候选数量。",
                "default": 80
              },
              "semantic_weight": {
                "type": "float",
                "description": "关系意图语义权重",
                "hint": "关系意图命中时段落语义召回的权重。",
                "default": 0.45
              },
              "sparse_weight": {
                "type": "float",
                "description": "关系意图稀疏权重",
                "hint": "关系意图命中时 BM25/稀疏召回的权重。",
                "default": 0.15
              },
              "graph_weight": {
                "type": "float",
                "description": "关系意图图谱权重",
                "hint": "关系意图命中时图谱向量召回的权重。",
                "default": 0.4
              },
              "return_relation_items": {
                "type": "bool",
                "description": "返回独立关系项",
                "hint": "关系意图命中时是否把关系结果作为独立结果返回。",
                "default": false
              }
            }
          }
        }
      },
      "temporal": {
        "type": "object",
        "description": "时序检索 —— 按时间范围查询记忆",
        "hint": "时序检索允许工具和 WebUI 按时间范围查询记忆，支持自然语言时间表达。",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用时序检索功能",
            "hint": "关闭后时序检索工具和 WebUI 时序查询将不可用。",
            "default": true
          },
          "default_top_k": {
            "type": "int",
            "description": "时序检索默认返回条数",
            "hint": "执行时序检索时，默认返回的记忆条数。建议范围 5~30。",
            "default": 10
          }
        }
      },
      "auto_route": {
        "type": "object",
        "description": "自动选择搜索方式 —— 根据提问内容自动选择普通检索、时间检索或混合检索",
        "hint": "开启后，插件会自动判断你的问题适合用哪种搜索方式。例如问「昨天说了什么」会自动使用时间搜索，问「我喜欢吃什么」会使用语义搜索。",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "自动选择搜索方式",
            "hint": "关闭后所有搜索都只用语义匹配，不再自动识别时间相关的问题。",
            "default": true
          },
          "enable_time_intent": {
            "type": "bool",
            "description": "识别自然语言中的时间",
            "hint": "开启后，可识别「昨天」「上周」「这个月」「从昨天到今天」等日常时间表达。",
            "default": true
          }
        }
      },
      "aggregate": {
        "type": "object",
        "description": "聚合检索 —— 混合普通检索、时序检索与事件摘要结果",
        "hint": "将语义搜索、时间搜索和事件摘要搜索的结果混合排序。一般保持默认即可。",
        "items": {
          "rrf_k": {
            "type": "int",
            "description": "RRF 融合 K 值",
            "hint": "控制不同搜索通道结果的混合方式。数值越大，各通道的结果排名越均匀；越小，排名靠前的结果权重越高。一般不需要调整。",
            "default": 60
          },
          "weights": {
            "type": "object",
            "description": "各检索通道权重",
            "hint": "调整不同搜索方式在最终结果中的重要程度。数值越大该通道越重要。一般保持默认的 1.0 即可。",
            "items": {
              "search": {
                "type": "float",
                "description": "语义检索权重",
                "hint": "语义搜索结果在最终融合中的权重。调高则更重视「意思相近」的匹配结果。",
                "default": 1.0
              },
              "time": {
                "type": "float",
                "description": "时序检索权重",
                "hint": "按时间范围搜索结果在最终融合中的权重。调高则更重视时间维度的匹配。",
                "default": 1.0
              },
              "episode": {
                "type": "float",
                "description": "事件摘要搜索权重",
                "hint": "事件摘要搜索结果在最终融合中的权重。调高则更重视整段事件的匹配。",
                "default": 1.0
              }
            }
          }
        }
      }
    }
  },
  "memory": {
    "type": "object",
    "description": "记忆生命周期 —— 控制记忆的自动衰减、冻结、剪枝和回收机制",
    "hint": "Memorix 的记忆不是永久存储，而是有生命周期的：活跃记忆会按半衰期自动衰减权重，低于阈值的进入冻结期等待唤醒，最终未被唤醒的将被剪枝回收。这套机制让记忆系统自动保留重要内容、淘汰过时信息。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用记忆自动维护",
        "hint": "关闭后记忆永不衰减和清除，所有记录永久保留。长期运行可能导致记忆库膨胀影响性能，建议保持开启。",
        "default": true
      },
      "half_life_hours": {
        "type": "float",
        "description": "记忆半衰期（小时）",
        "hint": "记忆权重衰减到一半所需的时间。设为 24 表示 1 天后权重降为一半、2 天后降为 1/4。值越大记忆保持越久，越小遗忘越快。建议 6~168。",
        "default": 24.0
      },
      "base_decay_interval_hours": {
        "type": "float",
        "description": "衰减计算间隔（小时）",
        "hint": "系统多久执行一次衰减计算。越小衰减越平滑但 CPU 开销略大。建议 0.5~6。",
        "default": 1.0
      },
      "prune_threshold": {
        "type": "float",
        "description": "清除阈值",
        "hint": "记忆权重低于此值时会被移入回收站。默认 0.1 表示权重降到 10% 以下时清除。值越大清除越激进，越小保留越多。范围 0~1。",
        "default": 0.1
      },
      "freeze_duration_hours": {
        "type": "float",
        "description": "冻结保留时长（小时）",
        "hint": "记忆权重降到阈值以下后，不会立刻被清除，而是先进入冻结状态。在此期间如果被再次提及则恢复活跃。值越大复活机会越多。建议 6~72。",
        "default": 24.0
      },
      "max_weight": {
        "type": "float",
        "description": "记忆权重上限",
        "hint": "单条记忆关系的权重上限，防止频繁强化的记忆权重无限增长。建议 5~20。",
        "default": 10.0
      },
      "auto_protect_ttl_hours": {
        "type": "float",
        "description": "手动强化后的保护时间（小时）",
        "hint": "通过管理面板或工具手动强化某条记忆后，在这段时间内它不会被遗忘或清除。建议 6~168 小时。",
        "default": 24.0
      }
    }
  },
  "person_profile": {
    "type": "object",
    "description": "人物画像 —— 自动提取并记录对话中出现的人物信息",
    "hint": "人物画像系统会从消息中提取人物的特征信息（如兴趣、性格、习惯等），在 LLM 请求时自动注入作为上下文参考，让机器人更了解对话对象。画像支持定期自动刷新，也可手动覆盖和清除。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用用户画像",
        "hint": "关闭后机器人不会记住用户的个人特征。",
        "default": true
      },
      "profile_ttl_minutes": {
        "type": "int",
        "description": "画像更新间隔（分钟）",
        "hint": "用户画像生成后会缓存一段时间，过期后下次需要时会重新生成。数值越大更新越少，越小画像越新。默认 360 分钟。建议 60~1440。",
        "default": 360
      },
      "top_k_evidence": {
        "type": "int",
        "description": "生成画像时参考多少条记忆",
        "hint": "生成用户画像时，从记忆库里取多少条相关记忆作为参考材料。越多画像越全面，但也越费 Token。建议 5~20。",
        "default": 12
      },
      "injection_max_profiles": {
        "type": "int",
        "description": "画像注入上限",
        "hint": "每次 LLM 请求前最多注入多少个人物画像。群聊中会优先包含当前发送者，并尝试包含被 @ 或被回复的人物。",
        "default": 3
      }
    }
  },
  "summarization": {
    "type": "object",
    "description": "对话总结策略 —— 控制对话总结的数据来源、长度和知识类型",
    "hint": "机器人会定期将聊天内容总结成精炼的摘要存入记忆。这比直接存储原始对话更高效，能在有限的上下文窗口中携带更多信息。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用对话总结",
        "hint": "关闭后不再生成任何总结。聊天记录仍会被记录，但不会被提炼成摘要知识。",
        "default": true
      },
      "source_mode": {
        "type": "string",
        "description": "总结的聊天记录来源",
        "hint": "transcript（推荐）：使用插件自己记录的聊天记录，独立可靠；astrbot：使用 AstrBot 自带的会话历史；hybrid：优先用 AstrBot 的，失败了自动切换到插件自己的。",
        "default": "transcript",
        "options": [
          "transcript",
          "hybrid",
          "astrbot"
        ]
      },
      "context_length": {
        "type": "int",
        "description": "每次总结多少条消息",
        "hint": "每次做总结时，取最近多少条聊天消息给 LLM 总结。越多总结越全面，但越费 Token。建议 20~200。",
        "default": 50
      },
      "default_knowledge_type": {
        "type": "string",
        "description": "总结生成的知识类型",
        "hint": "narrative（叙事型）：按时间线梳理对话脉络，适合日常闲聊；factual（事实型）：提取关键事实和结论，适合信息密度高的对话；mixed（混合型）：同时提取叙事和事实；structured（结构化）：生成结构化的知识条目；auto（自动）：由 LLM 自动判断最适合的类型。",
        "default": "narrative",
        "options": [
          "narrative",
          "factual",
          "mixed",
          "structured",
          "auto"
        ]
      },
      "auto_import": {
        "type": "object",
        "description": "自动总结",
        "hint": "聊天过程中自动触发总结。只有攒够足够多的新消息、并且过了冷却时间才会触发。",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用自动总结",
            "hint": "关闭后不再自动总结，只能通过手动或定时任务触发。",
            "default": true
          },
          "after_reply_only": {
            "type": "bool",
            "description": "只在机器人回复后才尝试总结",
            "hint": "开启后，只有机器人成功回复了一条消息后才会检查是否需要总结。关闭后用户发消息时也可能触发。",
            "default": true
          },
          "min_new_messages": {
            "type": "int",
            "description": "触发总结需要的最少新消息数",
            "hint": "上次总结之后至少要新增多少条消息才会再次自动总结。数值越大总结越少，越小总结越频繁。",
            "default": 12
          },
          "cooldown_minutes": {
            "type": "int",
            "description": "两次自动总结的最短间隔（分钟）",
            "hint": "同一个聊天里两次自动总结至少间隔多久。防止聊天频繁时反复触发总结浪费资源。",
            "default": 30
          }
        }
      }
    }
  },
  "ingest": {
    "type": "object",
    "description": "消息记录",
    "hint": "控制哪些聊天消息会被记录到记忆系统中。默认会先记录聊天流水，再由自动总结提炼成长期记忆。",
    "items": {
      "record_all_events": {
        "type": "bool",
        "description": "自动记录所有聊天消息",
        "hint": "开启后，群聊和私聊的消息都会自动被记录。关闭后只有通过 AI 工具手动写入的内容才会进入记忆。",
        "default": true
      },
      "skip_command_messages": {
        "type": "bool",
        "description": "忽略命令消息",
        "hint": "开启后，以 / 等命令前缀开头的消息不会被记录，避免把「/help」「/plugin」之类的命令混进记忆里。",
        "default": true
      },
      "command_prefixes": {
        "type": "list",
        "description": "命令前缀列表",
        "hint": "哪些开头的消息算作命令。比如 [\"/\", \"!\", \".\"] 表示以这三个符号开头的消息都不会被记录。",
        "default": [
          "/"
        ]
      },
      "memory_write_mode": {
        "type": "string",
        "description": "消息存储方式",
        "hint": "transcript_only（推荐）：先把聊天记录存为流水，积攒够了再由自动总结提炼成长期记忆；direct：每条消息都立刻存为长期记忆，可以马上被搜索到；both：和 direct 一样，同时保留流水账和即时写入；auto：智能模式，只有像「我叫小明」「我喜欢猫」这样的关键信息才会立刻存入长期记忆，其他走流水。",
        "default": "transcript_only",
        "options": [
          "transcript_only",
          "direct",
          "both",
          "auto"
        ]
      },
      "direct_write_assistant": {
        "type": "bool",
        "description": "同时记录机器人的回复",
        "hint": "仅在消息存储方式为 direct 或 both 时有效。开启后机器人自己的回复也会被存入长期记忆；关闭则只存用户的消息。",
        "default": true
      },
      "content_router": {
        "type": "object",
        "description": "智能消息过滤",
        "hint": "在消息存入记忆前，自动判断消息是否有价值。比如「哈哈」「嗯」这类无意义的消息可以被过滤掉，而「我是程序员」这类有信息量的内容会被优先保留。",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用智能消息过滤",
            "hint": "开启后根据消息内容判断是否有记忆价值，自动过滤无意义的消息。",
            "default": true
          },
          "drop_ephemeral_transcript": {
            "type": "bool",
            "description": "丢弃无意义的消息",
            "hint": "开启后，像「哈哈」「嗯」「ok」「👍」这类无实质内容的消息会被直接丢弃。关闭时这些消息仍然会记在流水里，只是不会写入长期记忆。",
            "default": false
          },
          "auto_direct_min_chars": {
            "type": "int",
            "description": "智能模式下的最短字数要求",
            "hint": "仅在消息存储方式为 auto 时有效。消息至少要有这么多字，才会被判定为「有价值」并立刻存入长期记忆。防止太短的消息被误判。",
            "default": 12
          }
        }
      },
      "skip_placeholder_only": {
        "type": "bool",
        "description": "忽略纯图片/表情/文件消息",
        "hint": "开启后，只包含 [图片]、[表情]、[转发消息]、[文件] 等占位符、没有文字的消息不会被记录。",
        "default": true
      },
      "max_message_chars": {
        "type": "int",
        "description": "单条消息最大长度",
        "hint": "超过这个字数的消息会被截断后再存储，防止超长消息占满记忆空间。",
        "default": 2000
      },
      "max_forward_fetch": {
        "type": "int",
        "description": "合并转发最多读取批次",
        "hint": "处理 QQ 合并转发消息时，最多读取多少批内容。数值越大读得越完整，但可能更慢。",
        "default": 8
      },
      "image_caption": {
        "type": "object",
        "description": "图片转述",
        "hint": "可选调用视觉模型把图片转成简短文字后再写入记忆。",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用图片转述",
            "hint": "开启后会调用视觉模型将图片转为文字描述存入记忆。",
            "default": false
          },
          "provider_id": {
            "type": "string",
            "description": "图片描述使用的 LLM 模型",
            "hint": "留空使用当前会话 Provider。",
            "default": "",
            "_special": "select_provider"
          },
          "max_count": {
            "type": "int",
            "description": "单条消息最多描述几张图片",
            "hint": "一条消息包含多张图时，最多对几张图生成描述。",
            "default": 1
          },
          "prompt": {
            "type": "string",
            "description": "图片描述提示词",
            "hint": "发给视觉模型的提示词，用于引导模型如何描述图片内容。",
            "default": "请简洁描述这张图片。"
          }
        }
      }
    }
  },
  "person_fact_writeback": {
    "type": "object",
    "description": "自动提取用户信息",
    "hint": "机器人回复后，自动从用户发言中提取稳定的个人事实（如「我是学生」「我喜欢打篮球」），写入长期记忆和用户画像。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用自动提取用户信息",
        "hint": "开启后，机器人回复后会自动从用户发言中提取个人事实（如兴趣、身份等）并写入记忆。",
        "default": true
      },
      "queue_maxsize": {
        "type": "int",
        "description": "待处理队列上限",
        "hint": "最多允许多少条待提取的消息排队等待处理。超过上限的新消息会被跳过。一般不需要调整。",
        "default": 256
      },
      "min_user_text_chars": {
        "type": "int",
        "description": "最短消息长度",
        "hint": "用户发的消息至少要有多少个字才会尝试提取信息。太短的消息（比如「嗯」「好的」）会被跳过。",
        "default": 4
      },
      "max_facts_per_turn": {
        "type": "int",
        "description": "每次最多提取几条信息",
        "hint": "从一条用户消息中最多提取多少条个人信息。",
        "default": 5
      },
      "max_registry_facts": {
        "type": "int",
        "description": "画像待确认事实条目上限",
        "hint": "限制画像中展示的待确认事实数量；完整事实与证据仍保留在账本中。",
        "default": 30
      },
      "max_evidence_chars": {
        "type": "int",
        "description": "提取时使用的消息长度上限",
        "hint": "发给 AI 分析的用户消息最多保留多少字。超长消息会被截断。",
        "default": 800
      },
      "update_registry_memory_points": {
        "type": "bool",
        "description": "在画像中展示待确认事实",
        "hint": "开启后，自动提取的信息以待确认事实进入画像。v1.2.0 起从事实账本投影，不再追加到旧 registry 字符串。",
        "default": true
      },
      "chat_provider_id": {
        "type": "string",
        "description": "信息提取使用的 LLM 模型",
        "hint": "可以为信息提取单独指定一个模型。留空则使用默认模型。",
        "default": "",
        "_special": "select_provider"
      },
      "temperature": {
        "type": "float",
        "description": "人物信息提取时的温度参数",
        "hint": "越低提取结果越稳定保守，越高越有创意但可能不准确，范围(0.0-1.0)。",
        "default": 0.1
      },
      "max_tokens": {
        "type": "int",
        "description": "最大输出token数",
        "hint": "一般不需要调整。",
        "default": 800
      }
    }
  },
  "web": {
    "type": "object",
    "description": "知识导入",
    "hint": "在管理面板中批量导入外部知识到记忆系统。支持上传文件、粘贴文本和扫描目录三种方式。",
    "items": {
      "import": {
        "type": "object",
        "description": "导入限制",
        "hint": "控制导入功能的各项限制，防止一次导入太多内容把系统拖慢。",
        "items": {
          "max_queue_size": {
            "type": "int",
            "description": "最大排队任务数",
            "hint": "同时能有多少个导入任务排队等待处理。超过后需要等前面的完成才能提交新的。建议 5~50。",
            "default": 20
          },
          "max_files_per_task": {
            "type": "int",
            "description": "单次导入最多几个文件",
            "hint": "一次导入操作最多允许包含多少个文件。建议 10~500。",
            "default": 200
          },
          "max_file_size_mb": {
            "type": "int",
            "description": "单个文件大小上限（MB）",
            "hint": "单个文件最大多少 MB。太大的文件解析时会很慢且占内存。建议 5~100。",
            "default": 20
          },
          "max_paste_chars": {
            "type": "int",
            "description": "粘贴导入最大字数",
            "hint": "通过粘贴文本方式导入时，最多允许多少个字符。",
            "default": 200000
          },
          "default_file_concurrency": {
            "type": "int",
            "description": "同时处理文件数（预留功能）",
            "hint": "此功能暂未启用，保持默认即可。",
            "default": 2
          },
          "default_chunk_concurrency": {
            "type": "int",
            "description": "分块并发数（预留功能）",
            "hint": "此功能暂未启用，保持默认即可。",
            "default": 4
          },
          "path_aliases": {
            "type": "object",
            "description": "可扫描的目录",
            "hint": "定义导入功能可以扫描哪些目录。出于安全考虑，只能访问这里定义的目录，不能扫描任意路径。路径相对于插件的数据存储目录。",
            "items": {
              "raw": {
                "type": "string",
                "description": "原始文件目录",
                "hint": "对应数据目录下的 raw 子文件夹。你可以把要导入的文件放到这个文件夹里，然后在导入面板中选择它。",
                "default": "raw"
              },
              "plugin_data": {
                "type": "string",
                "description": "插件数据目录",
                "hint": "对应插件的整个数据存储目录。默认值「.」表示根目录。可以改为子目录名来限制可扫描的范围。",
                "default": "."
              }
            }
          }
        }
      }
    }
  },
  "episode": {
    "type": "object",
    "description": "事件摘要 —— 从聊天段落异步生成可检索事件摘要",
    "hint": "机器人会自动把聊天内容按话题分段，生成「事件摘要」。例如问「上周发生了什么」时，机器人能给出完整的事件概述，而不是零散的句子。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用事件摘要",
        "hint": "关闭后不再生成和搜索事件摘要。",
        "default": true
      },
      "generation_enabled": {
        "type": "bool",
        "description": "后台自动生成事件摘要",
        "hint": "关闭后不再自动生成新的事件摘要，但已有的摘要仍可被搜索到。",
        "default": true
      },
      "generation_interval_seconds": {
        "type": "int",
        "description": "后台检查间隔（秒）",
        "hint": "后台每隔多少秒检查一次是否有新的聊天内容需要生成事件摘要。",
        "default": 30
      },
      "generation_batch_size": {
        "type": "int",
        "description": "每次处理的聊天来源数",
        "hint": "每轮检查时最多处理多少个不同的聊天来源（如不同的群）。",
        "default": 20
      },
      "max_retry": {
        "type": "int",
        "description": "生成失败重试次数",
        "hint": "事件摘要生成失败后最多重试几次。",
        "default": 3
      },
      "segmentation_model": {
        "type": "string",
        "description": "话题分段使用的模型",
        "hint": "auto（推荐）：使用当前默认的 AI 模型。也可以填写具体的模型名来指定。",
        "default": "auto"
      }
    }
  },
  "integration": {
    "type": "object",
    "description": "集成功能",
    "hint": "控制反馈纠错和模糊修正等需要管理员确认的高级能力。模糊修正默认启用但执行仍需确认。",
    "items": {
      "feedback_correction_enabled": {
        "type": "bool",
        "description": "启用反馈纠错",
        "hint": "开启后允许后台根据反馈任务尝试纠正长期记忆。",
        "default": false
      },
      "fuzzy_modify": {
        "type": "object",
        "description": "记忆模糊修正",
        "hint": "根据自然语言请求生成记忆修改计划。默认启用，与上游 A_memorix 对齐；执行仍需要管理员确认。",
        "items": {
          "enabled": {
            "type": "bool",
            "description": "启用模糊修正",
            "hint": "关闭时 preview/execute/list/get/rollback/reconcile 都会返回 fuzzy_modify_disabled。",
            "default": true
          },
          "candidate_limit": {
            "type": "int",
            "description": "候选记忆上限",
            "hint": "生成修改计划时最多收集多少条候选记忆。",
            "default": 20
          },
          "confirm_threshold": {
            "type": "float",
            "description": "自动执行置信阈值",
            "hint": "计划置信度低于该值时必须人工确认；默认保持保守。",
            "default": 0.85
          },
          "auto_execute_enabled": {
            "type": "bool",
            "description": "允许自动执行",
            "hint": "默认关闭。关闭时 execute 必须显式传 confirmed=true。",
            "default": false
          },
          "max_targets": {
            "type": "int",
            "description": "单个计划最大目标数",
            "hint": "限制一次模糊修正最多修改多少个目标，避免误伤过多记忆。",
            "default": 5
          },
          "allow_global_scope": {
            "type": "bool",
            "description": "允许全局作用域",
            "hint": "默认关闭，避免误把跨作用域的记忆纳入修改范围。",
            "default": false
          }
        }
      }
    }
  },
  "filter": {
    "type": "object",
    "description": "聊天过滤",
    "hint": "控制哪些群聊/私聊可以使用记忆功能。可以只允许特定聊天使用（白名单模式），或者排除某些聊天（黑名单模式）。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用聊天过滤",
        "hint": "关闭后所有聊天都可以使用记忆功能，不做任何限制。",
        "default": true
      },
      "mode": {
        "type": "string",
        "description": "过滤模式",
        "hint": "whitelist（白名单）：只有下面列出的聊天能使用记忆功能，其他一律不行。blacklist（黑名单）：下面列出的聊天不能用，其他都能用。",
        "default": "blacklist",
        "options": [
          "whitelist",
          "blacklist"
        ]
      },
      "chats": {
        "type": "list",
        "description": "聊天列表",
        "hint": "填写要允许/排除的聊天标识。格式示例：group:123456（群号）、user:789012（用户号）、stream:xxx（频道标识）。",
        "default": []
      }
    }
  }
}
